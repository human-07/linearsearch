#include<stdio.h>
#include<conio.h>
 void main()
{
 int a[20],n,key,i;
clrscr();
 printf("enter the size of array:");
 scanf("%d",&n);
 printf("enter the elements in array:");
 for(i=0;i<n;i++)
 {
 scanf("%d",&a[i]);
 }
 printf("enter the key to search:");
 scanf("%d",&kry);
 for(i=0;i<n;i++)
{
 if(a[i]==key)
}  
 printf("%d found at %d,key,i+1);
}
}
if(i== key)
 {
 printf("kry not found");
 }
getch();
}